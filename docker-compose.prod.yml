version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: wash-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: wash
      POSTGRES_PASSWORD: wash
      POSTGRES_DB: wash
    ports:
      - "15432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - wash-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U wash -d wash" ]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: wash-redis
    restart: unless-stopped
    ports:
      - "16379:6379"
    volumes:
      - redis_data:/data
    networks:
      - wash-network
    command: redis-server --appendonly yes
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wash-api
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 8000
      HOST: 0.0.0.0
      DATABASE_URL: postgresql://wash:wash@wash-postgres:5432/wash?schema=public
      REDIS_URL: redis://wash-redis:6379
      RUN_DB_MIGRATIONS: "true"
    ports:
      - "31800:8000"
    networks:
      - wash-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wash-worker
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://wash:wash@wash-postgres:5432/wash?schema=public
      REDIS_URL: redis://wash-redis:6379
      RUN_DB_MIGRATIONS: "false"
    command: ["node", "dist/workers/index.js"]
    networks:
      - wash-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: web-ui/Dockerfile
    container_name: wash-web
    restart: unless-stopped
    ports:
      - "31801:8001"
    networks:
      - wash-network
    depends_on:
      - api

networks:
  wash-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
